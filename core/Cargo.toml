[package]
name = "auracolor-core"
version = "1.0.0"
edition = "2021"
authors = ["Jonas Monteiro"]
description = "High-performance perceptual color science and contrast auditor core in Rust"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
